dependencyResolutionManagement {
	repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
	repositories {
		google()
		mavenCentral()
		mavenLocal()
	}
}
rootProject.name = "Mastodon"
include ':mastodon'

// masto.nyc fork: see fork-settings.gradle
apply from: 'fork-settings.gradle'
